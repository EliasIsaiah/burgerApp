<div class="burgerDiv">

    <a class="waves-effect waves-light btn" data-id="{{this.uuid}}"><i class="fas fa-hamburger"></i>{{this.burger_name}}</a>
    {{#unless this.devoured}}
    <a class="waves-effect waves-light btn" data-id="{{this.uuid}}">Devour</a>
    {{/unless}}

</div>